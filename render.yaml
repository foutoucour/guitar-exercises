services:
  - type: web
    name: guitar-exercises
    runtime: python
    plan: free
    buildCommand: make install
    startCommand: poetry run uvicorn guitar_exercises.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.14.3
